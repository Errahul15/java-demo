package com.demo.repository;

import com.demo.entity.Bus_Stop;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Bus_StopRepository extends JpaRepository<Bus_Stop, Long> {
}